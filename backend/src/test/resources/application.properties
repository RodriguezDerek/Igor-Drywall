spring.datasource.url=jdbc:h2:mem:db;DB_CLOSE_DELAY=-1
spring.datasource.username=sa
spring.datasource.password=sa
spring.datasource.driver-class-name=org.h2.Driver

spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=true
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.jpa.properties.hibernate.format_sql=true

# JWT Secret Key
jwt.secret.key=test-secret-key-test-secret-key-test-secret-key

# ADMIN Account Info
admin.email=test@test.com
admin.password=test123
admin.phoneNumber=1234567890

# Add these to your test properties so the S3Config bean can initialize
aws.access.key=fake-key
aws.secret.key=fake-secret
aws.region=us-east-1
aws.bucket.name=test-bucket